{
  "scripts": {
    "crawl:links": "bun run src/crawl-links.ts",
    "crawl:questions": "bun run src/fetch-questions.ts",
    "test": "bun test",
    "coverage": "bun test --coverage"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.0"
  }
}